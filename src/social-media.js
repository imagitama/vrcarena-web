// TODO: Move to utils

export function getUrlForVrChatAvatarId(avatarId) {
  return `https://vrchat.com/home/avatar/${avatarId}`
}

export function getUrlForVrChatWorldId(worldId) {
  return `https://vrchat.com/home/world/${worldId}`
}

export function getUrlForVrChatUserId(userId) {
  return `https://vrchat.com/home/user/${userId}`
}

export function getUrlForTwitterUsername(username) {
  return `https://twitter.com/${username}`
}

export function getUrlForYouTubeChannelByChannelId(channelId) {
  return `https://www.youtube.com/channel/${channelId}`
}

export function getUrlForTelegramUsername(username) {
  return `https://t.me/${username}`
}

export function getUrlForTwitchByUsername(username) {
  return `https://twitch.tv/${username}`
}

export function getUrlForGumroadUsername(username) {
  return `https://gumroad.com/${username}`
}

export function getUrlForPatreonByUsername(username) {
  return `https://patreon.com/${username}`
}

export function getUrlForBoothByUsername(username) {
  return `https://${username}.booth.pm`
}
